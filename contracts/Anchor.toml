[features]
seeds = false
skip-lint = false

[programs.devnet]
morra = "YOUR_PROGRAM_ID" # Replace with actual program ID

[programs.mainnet-beta]
morra = "YOUR_PROGRAM_ID" # Replace with actual program ID

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/" 